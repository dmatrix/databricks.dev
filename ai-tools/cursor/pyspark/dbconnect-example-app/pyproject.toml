[project]
name = "dbconnect-example-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "databricks-connect>=15.0.0",
]

[tool.uv.workspace]
members = [
    "venv",
]
